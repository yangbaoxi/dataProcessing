# sum() 方法对系列进行求和计算

# sum(iterable[, start])

# 参数
    # iterable -- 可迭代对象，如：列表、元组、集合。
    # start -- 指定相加的参数，如果没有设置这个值，默认为0。

# 返回值 返回计算结果。

sum([0,1,2])  # 3

sum((2, 3, 4), 1) # 10

sum([0,1,2,3,4], 2) # 12
