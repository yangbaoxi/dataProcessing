// 语法

// Array.toString();

// 说明

// array.toString() 返回值跟 join() 方法 默认参数 返回值的字符串一致 返回一个新数组，不改变原有数组

// 示例

let a = new Array('A','B','C');

let b = a.toString();

console.log(b);     // A, B, C